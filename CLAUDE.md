# CLAUDE.md — PQSec-Pipeline

## 프로젝트
PQC로 보호되는 mTLS 채널 위에 eBPF 보안 이벤트를 전송하고, LLM(Claude API)이 이상행위를 분류·설명하는 통합 엔드포인트 보안 파이프라인. 42dot Security System 포지션 포트폴리오용. 상세 계획은 `ROADMAP.md` 참조.

## 학습 모드 (가장 중요)
이 프로젝트는 학습이 목적이다. "구현 속도 위주, 개념은 핵심만" 원칙을 따른다.

1. **구현 전 개념 먼저.** 새 컴포넌트/알고리즘/라이브러리를 처음 도입할 때는, 코드를 쓰기 전에 다음을 3~5문장으로 먼저 설명하고 승인을 받는다:
   - 이게 뭐고 왜 필요한가
   - 대안 대비 왜 이걸 고르는가 (트레이드오프 한 줄)
   - 면접에서 물어보면 뭐라고 답해야 하는가
2. 승인("가자"/"진행") 받기 전에는 구현 코드를 작성하지 않는다. 스캐폴딩·빌드설정 같은 기계적 작업은 예외.
3. 개념 설명은 **핵심만.** 교과서식 나열 금지. 이 프로젝트 맥락에서 실제로 결정에 영향을 주는 것만.
4. 이미 재현이 잘 아는 영역(eBPF, C++, Claude API 멀티에이전트, 룰기반 프리필터)은 개념 설명 생략하고 바로 구현. 새 영역(PQC, 키교환 설계, mTLS record layer)에서만 개념을 짚는다.

## 워크플로우
- 큰 작업은 먼저 프로젝트 구조를 분석·보고하고 승인을 받은 뒤 구현한다. (기존 MapDisplayInhouse/Night Issue Analyzer와 동일 원칙)
- 주(Week) 단위로 진행. 각 주 시작 시 그 주 목표와 체크리스트를 확인하고, 끝날 때 데모 가능한 상태인지 점검.
- 막히면 우회 설계를 먼저 제안한다. 범위(scope)를 지키는 게 완벽함보다 우선.

## 기술 스택
- 언어: C++ (C++17 이상), CMake
- eBPF: libbpf + CO-RE, Ubuntu 24.04 / kernel 6.17
- PQC: liboqs (ML-KEM/Kyber), 고전 X25519와 하이브리드
- 대칭암호: AES-256-GCM, 키유도 HKDF
- LLM: Claude API (Haiku 1차 분류 → Sonnet 심층), 룰기반 프리필터 선행
- 배포: analyzer는 Docker 컨테이너

## 코딩 규칙
- 보안 코드다. 크립토는 직접 구현하지 말고 검증된 라이브러리(liboqs, libsodium/OpenSSL)의 primitive를 조합만 한다. "직접 만든 암호"는 절대 금지 — 조합/프로토콜 설계만 우리 몫.
- 에러 처리 명시적으로. 크립토 실패는 조용히 넘어가지 않는다 (fail-closed).
- 커밋 단위는 작게, 각 Week 산출물이 독립적으로 데모 가능하게.

## 범위 가드레일
`ROADMAP.md`의 "넣지 않는다" 목록을 지킨다. 완전한 TLS 재구현, 전체 PKI, 프로덕션급 eBPF 커버리지는 범위 밖. 통합 파이프라인이 관통해서 돌아가는 것이 핵심 가치.

## 면접 대비
`ROADMAP.md` Week 4의 Q&A 필수 항목 5개에 대한 답을 프로젝트 진행하며 `INTERVIEW_NOTES.md`에 누적한다. 각 답은 3문장 이내.
